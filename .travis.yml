language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t dhana555std/reactapp -f Dockerfile.dev .

script:
  - docker run -e CI=true dhana555std/reactapp npm run test -- --coverage